import 'package:flutter/material.dart';

class YpmAuthScreen extends StatelessWidget {
  const YpmAuthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('À implémenter')),
      body: const Center(child: Text('Contenu à venir...')),
    );
  }
}
